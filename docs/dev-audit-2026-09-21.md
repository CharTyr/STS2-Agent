# dev 分支审查与修复记录

> 历史快照：记录 2026-09-21 本轮审查与验证，不代表后续版本状态。

日期：2026-09-21。设备：CharBook。项目：STS2-Agent。

## 范围与起点

工作目录为 `C:/Users/chart/Documents/project/sp`。开始时分支为 `dev`，HEAD 为 `65c9ad9`，本地 `origin/dev` 同指该提交，版本元数据为 `0.14.5`。开始时工作区只有两个未跟踪文件：`cstest.log` 与 `v.log`；本次未编辑或暂存它们。

项目提供游戏内 AI 对话、自动游玩和本地双开 AI 队友。C# Mod 通过 GameThread 读取状态和执行动作；AgentRuntime/AgentLoop 负责模型、预算和会话；Godot overlay 管理玩家界面；loopback HTTP、原生 MCP 和 Python FastMCP 提供外部入口。审查以当前 dev 相对 main 的 25 个变更文件为起点，沿调用关系检查配置、队友连接、状态投影、事件流、模型请求和离线验证路径。

本轮没有启动游戏、安装 Mod 到游戏目录、调用真实模型或发布版本。已有历史实机记录不作为这批改动的实机证据。

## 已确认的问题与修复

| 编号 | 问题与影响 | 修复与主要位置 | 回归证据 |
| --- | --- | --- | --- |
| 01 | 设置克隆漏掉 OverlayTheme，保存或重建设置时会恢复默认主题。 | `Config/SettingsClone.cs` 完整复制主题。 | `SettingsClone.CarriesEditableFields` 使用 ivory 而非默认值。 |
| 02 | 点击主题会提交表单中其他未保存的改动，重建表单会丢失半输入内容；保存失败也没有局部处理。 | `Ui/AgentOverlayHost.Settings.cs` 仅保存已存配置中的主题，保留原控件，只重建色块；处理 IO 错误并为端点/模型面板记录重绘角色。 | `DevAudit.ThemeSaveDoesNotHarvestOtherEdits`、现有 overlay 契约和 Mod 构建。视觉结果仍需实机检查。 |
| 03 | 队友摘要读取 companion 进程却筛选非本地玩家，显示主机的生命/能量与 AI 的手牌。 | `Multiplayer/TeammateStatus.cs` 筛选 companion 的 `is_local=true`。 | 六个队友测试按真实数据来源修正，覆盖战斗、地图、倒下与缺失状态。 |
| 04 | Python 已注册的 `diff_state` 工具调用同名内层函数，触发无限递归。 | `mcp_server/src/sts2_mcp/server.py` 用别名调用纯函数。 | 直接执行 guided/layered/full 三种配置的工具入口；修复前均 RecursionError。 |
| 05 | 差异数刚好等于上限时，把未变化路径也算作被截断内容。 | 两面的 `StateViews` 只在发现额外差异时报告条数截断。 | C# / Python exact-cap 测试。 |
| 06 | 超过深度上限的子树全部变成相同占位值，实际变化被报告为完整相等。 | 两面明确标记深度未完整比较，`truncated=true`。 | C# / Python 深层变化测试；更新工具说明。 |
| 07 | 字段名中的点、方括号与嵌套路径冲突，字典覆盖后漏报变化。 | 保留常规字段路径；特殊字段使用 JSON 引号括号路径。 | 两面 `a.b` 字段与嵌套 `a.b` 同时存在的测试。 |
| 08 | Python 把空对象 `{}` 与字符串 `"{}"` 当作相同叶节点。 | 容器、数组长度、深度标记与字符串使用不同内部类型。 | 两面的空对象/字符串回归。 |
| 09 | C# diff 经 double 输出大整数会改写末位，且把 1 与 1.0 误判为变化。 | `Agent/StateViews.cs` 保留 JsonElement 数值，按精确十进制形式比较。 | 大整数、等值数值、负零与极小数测试。 |
| 10 | 最后一个订阅离开后，尚在执行的采样又写回空闲期快照。 | `EventStreamSubscribers.cs` 与 `GameEventService.cs` 只为活跃订阅保留/处理快照。 | idle/in-flight 回归与已有订阅顺序测试。 |
| 11 | 满队列移除最后一个订阅时，旧快照及部分轮询需求未清除；新生命周期还可能沿用去重状态。 | 普通发布及快照发布均清理状态、同步需求；首个采样重置去重器。 | 普通帧与快照帧分别溢出队列的回归。 |
| 12 | SSE 连接重置、截断响应或读不出错误正文时抛出裸异常；连接超时也可能被当作空闲读取。 | `mcp_server/src/sts2_mcp/client.py` 统一结构化错误，区分连接/读取超时，连接和读取均受总期限限制。 | reset、IncompleteRead、HTTP 错误正文、连接超时、连接期限测试。 |
| 13 | 服务端连续关闭空事件流时，客户端立即重连形成忙循环。 | 以不超过剩余期限的 100 ms 间隔重连。 | 假时钟验证重连次数及总期限。 |
| 14 | 保存失败后重试成功，SettingsStore 仍保留 save_failed 提示。 | 成功保存清除过期失败提示，保留原有文件恢复告知。 | `DevAudit.SaveRetryClearsFailureNotice`。 |
| 15 | 模型连通测试吞掉用户取消并生成失败记录，取消也可能被视觉/读取工具当作普通错误。 | `AgentLoop.cs` / `AgentRuntime.cs` 传播调用方取消，并显示独立取消状态。 | 预先取消、请求中取消、服务端自身失败仍能报告的测试。 |
| 16 | CompanionConnection 返回完整 HTTP envelope，队友解析器却读取外层，真实调用会得到空摘要。 | `TeammateStatus.Parse` 解包成功响应，并兼容直接状态数据；拒绝错误或无效 envelope。 | `TeammateStatus.HttpEnvelope` / `RejectsErrorEnvelope`，同时核对 Router 的实际输出结构。 |

另更新当前源码基线，并纠正 SSE 编号说明：编号全局分配，单个订阅不保证连续，不能仅凭编号跳变推断广播丢失。

## 验证记录

基线：C# 572 项通过；Python 304 项通过。首批回归在未改生产代码时复现 17 个 C# 失败，Python 6 个测试中出现 5 个失败与 2 个子测试异常。第二批单独复现两个模型取消失败及三个配置下的 MCP 递归异常。

第一轮修复后，C# 583 项通过，Mod Release 构建成功且没有警告或错误。Python 发现一个假时钟随读取次数流逝的旧测试；已改为模拟 IO 消耗时间，并继续要求两次空闲读取超时与准确总期限。

最终完整离线预检于 2026-09-21 通过：`powershell.exe -NoProfile -NonInteractive -ExecutionPolicy Bypass -File scripts/preflight-release.ps1` 退出码为 **0**，全部 **16 个预检步骤**完成。

- Mod Release 构建成功，0 警告、0 错误。
- C# **589 PASS / 0 FAIL**，比基线增加 17 项；Python **315 项 OK**，比基线增加 11 项。
- 12 道验证检查全部通过；57 个动作、35 种屏幕、488 个状态字段、12 条路由及生成的 OpenAPI 契约保持一致。
- MCP 工具分档、预算代理离线自测、版本元数据、打包源码契约、API schema、决策基准、验证检查自测、PowerShell 失败传播和隔离配置种子测试通过。
- C# 与 Python 状态差异对照 **165 组 / 0 差异**，包含数字、路径、Unicode、条数上限及深度边界。
- `git diff --check` 通过。

日志和机器可读结果保存在本机 `build/dev-audit-2026-09-21/`：`preflight.log`、`verification-summary.json`，以及 `parity/` 下的输入与两面比较结果。该目录遵循项目原有 build 忽略规则，未纳入源码提交。

过程记录：首轮预检发现本轮新增翻译项的语法错误，已按现有 map 赋值语法修正。一次 PowerShell 重定向把 unittest 正常写入 stderr 的进度当作终止错误，改为 OS 层捕获后得到完整结果。文档检查还发现审查页缺少历史快照标记，补齐后通过。两面 Unicode 路径比较最初出现 22 处转义差异，统一转义格式并添加回归后，165 组全部一致。

## 需要实机确认的行为

检查主题切换时的配色、焦点和滚动位置；观察主机/AI 生命值不同时的队友面板；在真实界面切换、断开与重连时检查事件顺序；测试自动游玩、视觉请求取消与本地双开完整对局。本轮离线通过不代表这些实机路径已经验收。
