# Agent turn integrity

User authorizes fixing all five findings from build/dev-audit-followup-2026-09-21/report.md. Baseline dev: 00aad14. No further approval needed for implementation.

## Acceptance

An accepted action cannot be replayed in the same decision because observation failed. Known in-turn tokens, including vision, stop the next model request at the cap. Cancellation preserves completed actions and spent requests without scheduling more work. Proactive chat sees the play turn's usage and neither turn is counted twice. Non-object act parameters produce a recoverable tool error. Preserve existing HTTP/MCP fields, cancellation type compatibility and settings. Validate offline, commit locally, do not launch/deploy games, call providers, delegate to dsh or push. Preserve cstest.log and v.log.
